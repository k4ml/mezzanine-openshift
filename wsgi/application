#!/usr/bin/python

import os, sys
os.environ['DJANGO_SETTINGS_MODULE'] = 'myblog.settings'
sys.path.append(os.environ['OPENSHIFT_HOMEDIR'] + '/django/repo/wsgi/myblog')
import django.core.handlers.wsgi
application = django.core.handlers.wsgi.WSGIHandler()
